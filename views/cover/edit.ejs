
<h2>가림판 설정</h2>
<div id="edit">
    <!-- 좌측: 설정 영역 -->
    <div id="coverEditForm">            
        <div class="setting-group">
            <label>가림판 이름</label>
            <input class="inputBox" id="cover-title" type="text" placeholder="가림판 이름을 입력해주세요" required>
        </div>
        <div class="setting-group">
            <label>배경 이미지</label>
            <div class="file-control">
                <input class="inputBox" id="imageUpload" type="file">
                 <div id="deleteImageBtn" title="이미지 삭제" >
                    <svg id="deleteImageBtn" xmlns="http://www.w3.org/2000/svg" width="32" height="32" viewBox="0 0 24 24"><path fill="currentColor" d="m9.4 16.5l2.6-2.6l2.6 2.6l1.4-1.4l-2.6-2.6L16 9.9l-1.4-1.4l-2.6 2.6l-2.6-2.6L8 9.9l2.6 2.6L8 15.1zM7 21q-.825 0-1.412-.587T5 19V6H4V4h5V3h6v1h5v2h-1v13q0 .825-.587 1.413T17 21z"/></svg>
                </div>
            </div>
        </div>

        <div class="setting-group">
            <div class="setting-groups">
                <label for="coverBackgroundColor">배경색:</label>
                <input id="coverBackgroundColor" type="color" value="#ff0000">
            </div>
            <div class="setting-groups">
                <label>투명도:</label>
                <input id="coverOpacity" type="range" min="0" max="1" step="0.01" value="0.87">
            </div>
        </div>

        <div class="setting-group">
            <label>문구 입력:</label>
            <textarea class="inputBox" id="textInput" name="question" placeholder="가림판 위에 띄우고 싶은 문구를 입력하세요"></textarea>

            <div class="setting-groups">
                <label>문구 크기:</label>
                <input id="textSize" type="number" min="10" max="100" value="16">
            </div>
            <div class="setting-groups">
                <label>문구 색상:</label>
                <input id="textColor" type="color" value="#000000">
            </div>
        </div>

        <div class="setting-group">
            <div class="setting-groups">
                <label>문제 색상:</label>
                <input id="questionColor" type="color" value="#000000">
            </div>
            <div class="setting-groups">
                <label>답 색상:</label>
                <input id="answerColor" type="color" value="#ff4e00">
            </div>
            <div class="setting-groups">
                <label>답 투명도:</label>
                <input id="answerOpacity" type="range" min="0" max="1" step="0.01" value="0.83">
            </div>
        </div>
    </div>

    <!-- 우측: 임시 가림판 -->
    <!-- <h3>가림판 미리보기</h3> -->
    <div id="previewWrapper">
    <div id="previewText">
        <a id="sample-question">실제 표시될 답 :</a>
        <a id="sample-answer">answer</a>
    </div>
    <div id="coverPreview">
        <img id="previewImage">
        <div id="decorationText"></div>
    </div>
</div>

</div>
<button class="btn" id="saveBtn">설정 저장</button>
