<div class="folder-grid">
  <% if (notes && notes.length > 0) { %>
    <% notes.forEach(note => { %>
      <div class="folder-item" id="<%= note.note_id %>">
        <a class="folder-link" href="/cards_<%= note.template %>">
          <img src="/images/folder.jpg" alt="폴더">
        </a>
        <%= note.title %>
      </div>
    <% }) %>
  <% } else { %>
  </div>
  <p>노트가 없습니다.</p>
  <% } %>
